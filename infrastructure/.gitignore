# Terraform state files - should never be committed
*.tfstate
*.tfstate.*
*.tfplan
.terraform/
.terraform.lock.hcl

# Node modules - should never be in infrastructure folder
node_modules/
@*/
.bin/

# Build artifacts - Lambda deployment packages
*.zip

# Package files that shouldn't be here
.package-lock.json
package-lock.json
package.json
yarn.lock

# Temporary and log files
*.tmp
*.log
*.bak
*.backup

# OS files
.DS_Store
Thumbs.db

# AWS credentials - never commit
*.pem
credentials
